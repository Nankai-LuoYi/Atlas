# 时期场景与 OPC 来源表达

2026-09-23

## 内容与边界

- 对照本地 Di Bella 2021 正文第 1–3 页、Fig. 1–3：早期祖细胞、Cajal–Retzius 观察、连续表达变化、E13.5 早期分支、E14.5–E17.5 重叠生成时段、E15.5 径向迁移状态、E17.5 胶质群体观察、出生后亚型分化。
- P0 沿用本站独立 Evidence 的 EOMES 表达观察，不将追踪后代、干预表型画成正常 P0 状态。
- E16.5 采用连续分化语境，不增加独有事件或空间数据。
- 插图为 AI 辅助阅读示意，不是组织切片重建、逐日形态数据库或细胞比例图。蓝紫色不代表实际染色或亚型识别标准。图中长短、分枝、密度不能作为实测结论。
- 场景相关对象只是阅读入口，不写入 Cell/Relation/Event/Evidence，不建立新 stageEvidenceLinks，不改变 coverage。
- 背侧与特定腹侧来源 OPC 是来源分类，现有整理没有可用于该界面的来源特异形态判据；使用共享 OPC 图和不同来源标签，不据此宣称两群细胞生物学完全相同。

## 实现

- `dist/app.js`：stageScenes 为独立展示映射；12 个时期各对应一个场景视窗。跨时期关系模型通过原生 details 展开，relation 深链接自动展开；原科学 state 与 URL 规则保留。
- `dist/style.css`：时期场景、来源标签、折叠入口及响应式样式。
- `dist/assets/胚胎早期发育场景.png`：E10.5/E11.5/E12.5/E13.5，按左上、右上、左下、右下排列。
- `dist/assets/神经发生与迁移场景.png`：E14.5/E15.5/E16.5/E17.5，同上顺序。
- `dist/assets/围出生期发育场景.png`：E18.5/P0/P1/P4，同上顺序。
- 使用内置 imagegen 生成，原图保留；CSS 视窗显示单幅场景。围出生期上排视窗底部稍作遮裁，避免下排边缘串入。
- 完整生成提示词：`output/时期场景核对/生成提示词.json`。
- 本地验收脚本和截图：`output/时期场景核对/`；该目录不提交，正式资源在 dist。

## 2026-09-24 图例与品牌调整

- 每幅时期场景增加编号定位和中文图例；标出代表性绘图角色，不逐个鉴定细胞，不推断新的 marker、谱系或时期证据。
- 删除时期摘要下方的说明、Evidence 行和图号文字；证据保留在右侧详情及原有模型对象中。
- 四页导航及 favicon 统一使用 `dist/assets/小鼠神经图谱标志.png`，由内置 imagegen 生成，原 SVG 文件保留。
- 图标完整提示词：`output/插图标注核对/图标生成提示词.txt`。
- 验收脚本和截图：`output/插图标注核对/`，包括 12 个时期图例数量匹配、4 页图标加载、响应式、历史导航、Evidence 和搜索。
